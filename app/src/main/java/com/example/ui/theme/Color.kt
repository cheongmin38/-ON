package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Comforting Warm Care Theme Palette
val WarmPrimary = Color(0xFFFF6F00)       // Bright Orange Amber (high visibility, action-oriented)
val WarmSecondary = Color(0xFF00796B)     // Healing Teal (calming, stable, supportive)
val WarmTertiary = Color(0xFF4527A0)      // Reassuring Deep Purple (distinguished and elegant)
val WarmBackground = Color(0xFFFCF9F2)    // Warm, comforting ivory/cream background (soft on eyes)
val WarmSurface = Color(0xFFFFFBF4)       // Comforting soft cream surfaces for components
val WarmOnSurface = Color(0xFF1E1C1A)     // Soft dark charcoal for text (excellent high contrast)
val WarmAlert = Color(0xFFC62828)         // Clear high-contrast danger red for SOS

// Soft pastels/highlights
val WarmActiveGreen = Color(0xFF2E7D32)
val WarmLightTeal = Color(0xFFE0F2F1)
val WarmLightAmber = Color(0xFFFFF3E0)
val WarmLightRed = Color(0xFFFFEBEE)
val WarmGrey = Color(0xFF5C544F)
val WarmBorder = Color(0xFFEFE8E1)
